import streamlit as st

st.set_page_config(
    page_title="WB Analytics",
    page_icon="🛍️",
    layout="wide",
    initial_sidebar_state="expanded",
)

st.title("WB Analytics")
st.markdown(
    """
Приватная панель для контроля цен и финансовой аналитики Wildberries.

**Главная логика системы:**

* `Price Lock` жестко возвращает продавцовую цену WB к зафиксированному значению.
* Финансовая аналитика считает фактическую экономику по отчетам WB.
* Рекомендованные цены показываются как подсказки и не применяются автоматически.

Используйте боковое меню:

* **Price Lock** — контроль фиксированной цены и история корректировок.
* **Settings** — себестоимость, минимальная прибыль, фиксированная цена и скидка.
* **Analytics** — P&L, динамика, unit-экономика и рекомендованная цена в таблице.
"""
)
